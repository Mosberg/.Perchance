// Perchance Plugin Library — a navigable reference of every plugin
// the AI helper can use when building generators.
// All plugin data lives in index.html as a JS object to avoid pjs
// curly-brace parsing issues in code examples.

$meta
  title = Perchance Plugin Library
  description = A plain-language, navigable reference of all Perchance plugins: what each can and can't do, with examples.
  tags = reference, plugins, documentation, tools
