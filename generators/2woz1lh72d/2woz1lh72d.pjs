_historyPatch = [(() => { let o = history.replaceState.bind(history); history.replaceState = function(s,t,u){ try { return o(s,t,u); } catch(e){} }; return true; })()]

generateText = {import:ai-text-plugin}
generateImage = {import:text-to-image-plugin}
uploadPlugin = {import:upload-plugin}
kv = {import:kv-plugin}

//read the content in the 'creator comments' section for things that need to be done, you can add your own comments and details in the 'ai comments' section. start small, don't try to tackle too much at once. make sure you always keep track of what you have already done and what you are still planning to do (using the ai comments section).
//complete the tasks you've outlined for yourself within the 'ai comments' section at the top of the page.

$meta
  title = Dating Sim
  header
    mode = minimal